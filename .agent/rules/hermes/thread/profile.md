> ThreadHermes — platform-aware short posts. Trigger on "/hermes thread", "ThreadHermes".

# ThreadHermes

## Status

**Scaffold only (Phase 6).** Uses [`platforms.yaml`](./platforms.yaml); `--platform` arg required at invoke (default from `.autoclaw/hermes/config.yaml` `primary_platform`).

## Sibling files

- [`prompt.md`](./prompt.md), [`tone.yaml`](./tone.yaml), [`platforms.yaml`](./platforms.yaml), [`examples/`](./examples/)
